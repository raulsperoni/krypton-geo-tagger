version: "3.1"
services:


  backup:
    image: krypton_db
    command: /opt/krypton-db/mongo_backup.sh
    depends_on:
      - mongo
    environment:
      - AUTH=no
      - MONGO_GEO_HOST=bd_mongo


volumes:
  mongo_data_vol:
